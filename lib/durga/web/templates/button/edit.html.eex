<h2>Edit Button</h2>

<%= render "form.html", changeset: @changeset,
                        action: button_path(@conn, :update, @button),
                        nodes: @nodes %>

<span><%= link "Back", to: button_path(@conn, :index) %></span>
