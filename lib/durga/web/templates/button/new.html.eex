<link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
<h2>New Button</h2>

<%= render "form.html", changeset: @changeset,
                        action: button_path(@conn, :create),
                        node_id: @node_id,
                        nodes: @nodes %>

<span><%= link "Back", to: node_path(@conn, :show, @node_id) %></span>
