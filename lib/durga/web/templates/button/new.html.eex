<h2>New Button</h2>

<%= render "form.html", changeset: @changeset,
                        action: button_path(@conn, :create),
                        nodes: @nodes %>

<span><%= link "Back", to: button_path(@conn, :index) %></span>
